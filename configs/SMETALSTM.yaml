METRLA:
  num_nodes: 207
  in_steps: 12
  out_steps: 12

  train_size: 0.7
  val_size: 0.1
  
  lr: 0.001
  weight_decay: 0
  milestones: [10, 40]
  clip_grad: False
  batch_size: 64
  max_epochs: 200
  use_cl: False

  load_npz: False
  with_embeddings: True
  
  model_args:
    num_nodes: 207
    out_steps: 12
    lstm_input_dim: 1
    lstm_hidden_dim: 64
    s_embedding_dim: 64
    learner_hidden_dim: 64

PEMSBAY:
  num_nodes: 325
  in_steps: 12
  out_steps: 12

  train_size: 0.7
  val_size: 0.1
  
  lr: 0.001
  weight_decay: 0
  milestones: [10, 40]
  clip_grad: False
  batch_size: 64
  max_epochs: 200
  use_cl: False

  load_npz: False
  with_embeddings: True
  
  model_args:
    num_nodes: 325
    out_steps: 12
    lstm_input_dim: 1
    lstm_hidden_dim: 64
    s_embedding_dim: 64
    learner_hidden_dim: 64
  